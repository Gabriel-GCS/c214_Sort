public abstract class Opcoes {

    protected Interface interface;

    public int[] sort(int [] lista){
        return interface.sort(lista);
    }

    public Interface getInterface() {
        return interface;
    }
}